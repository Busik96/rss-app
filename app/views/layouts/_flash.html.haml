- flash.each do |key, value|
  .row
    .col-sm-12
      %div{:class => "#{flash_class(key)} alert-dismissible fade show mt-3", :role => "alert"}
        = value
        %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
          %span{"aria-hidden" => "true"} ×
